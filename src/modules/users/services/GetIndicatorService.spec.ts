// import FakeUnilevelNodesRepository from '../../unilevel/repositories/fakes/FakeUnilevelNodesRepository';
// import FakeUsersRepository from '../repositories/fakes/FakeUsersRepository';
// import GetIndicatorService from './GetIndicatorService';

// let fakeUsersRepository: FakeUsersRepository;
// let fakeUnilevelNodesRepository: FakeUnilevelNodesRepository;
// let getIndicatorService: GetIndicatorService;
describe('GetIndicatorService tests', () => {
  beforeEach(() => {
    // fakeUsersRepository = new FakeUsersRepository();
    // // fakeUnilevelNodesRepository = new FakeUnilevelNodesRepository();
    // getIndicatorService = new GetIndicatorService(
    //   fakeUsersRepository,
    //   // fakeUnilevelNodesRepository,
    // );
  });
  it('should throw an error', async () => {
    // await expect(getIndicatorService.execute('test')).rejects.toHaveProperty(
    //   'message',
    //   'Indicador nao encontrado',
    // );
  });
  // it('should throw an error', async () => {
  //   await fakeUsersRepository.create({
  //     email: 'test',
  //     full_name: 'test',
  //     phone_number: 'test',
  //     username: 'test',
  //   });
  //   await expect(getIndicatorService.execute('test')).rejects.toHaveProperty(
  //     'message',
  //     'Indicador nao possui rede unilevel',
  //   );
  // });
  it('should return a user', async () => {
    // await fakeUsersRepository.create({
    //   email: 'test',
    //   full_name: 'test',
    //   phone_number: 'test',
    //   username: 'test',
    // });
    // await fakeUnilevelNodesRepository.create({
    //   user_id: indicator.id,
    //   indicator_id: '1',
    // });
    // await expect(getIndicatorService.execute('test')).resolves.toHaveProperty(
    //   'id',
    // );
  });
});
